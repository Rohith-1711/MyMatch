# Supabase Configuration
# You can find these values in your Supabase Dashboard: Project Settings -> API
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# NOTE: Google Places and Tenor APIs have been removed to avoid costs/API signups for the MVP.
# We will use OpenStreetMap (Overpass API) for venue suggestions which is 100% free and requires NO API KEY.
# For GIFs, we can use Giphy's public beta key or drop the feature for the MVP.
